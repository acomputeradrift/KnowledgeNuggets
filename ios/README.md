# Knowledge Nuggets iOS

This folder contains SwiftUI source files for the iOS app. Create a new Xcode iOS App project named `KnowledgeNuggets` and drop these files into the target.

## Notes

- Update `APIClient.baseURL` to point to your backend.
- Wire up auth flows in `LoginView`/`SignupView`.
- Request notification permissions and register APNs token.
